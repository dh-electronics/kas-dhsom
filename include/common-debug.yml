header:
  version: 14

local_conf_header:
  debug_extra_image_features: |
    EXTRA_IMAGE_FEATURES:append = " allow-empty-password allow-root-login empty-root-password post-install-logging tools-debug tools-profile"
